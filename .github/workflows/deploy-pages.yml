name: Deploy Editor to GitHub Pages

on:
  push:
    branches:
      - main
    paths:
      - 'src-renderer-webpack/**'
      - 'font/**'
      - 'webpack.web.cjs'
      - 'package.json'
      - 'package-lock.json'
      - '.github/workflows/deploy-pages.yml'
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: true

jobs:
  build-and-deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          submodules: recursive
          persist-credentials: false

      - name: Install Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm

      - name: Install dependencies
        # 使用 --ignore-scripts 跳过 scratch-vm 的 prepublish（它会从 packagerdata.turbowarp.org
        # 下载 microbit 固件，经常超时），scratch-vm 已有预编译产物无需重新构建
        run: npm ci --ignore-scripts

      - name: Prepare extensions
        run: node scripts/prepare-extensions.mjs

      - name: Build web editor
        run: npm run web:build

      - name: Setup Pages
        uses: actions/configure-pages@v5

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: dist-web

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
